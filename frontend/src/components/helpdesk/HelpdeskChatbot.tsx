import React from "react";

interface HelpdeskChatbotProps {
  // Add any props you need for the chatbot
}

const HelpdeskChatbot: React.FC<HelpdeskChatbotProps> = () => {
  return (
    <div className="helpdesk-chatbot">
      {/* Content is handled by the parent HelpdeskWidget component */}
    </div>
  );
};

export default HelpdeskChatbot;
